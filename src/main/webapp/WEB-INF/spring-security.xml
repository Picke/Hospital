<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <!-- enable use-expressions -->
    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/" access="isAnonymous()" />
        <intercept-url pattern="/administration/*" access="hasRole('Admin')" />
        <intercept-url pattern="/registration/*" access="hasRole('Registrar')" />

        <!-- access denied page -->
        <access-denied-handler error-page="/403" />
        <form-login
                login-page="/"
                authentication-success-handler-ref="hospitalAuthenticationSuccessHandler"
                authentication-failure-url="/login?error"
                username-parameter="username"
                password-parameter="password" />
        <logout logout-success-url="/login?logout" />

    </http>

    <beans:bean id="hospitalAuthenticationSuccessHandler"
                class="com.bodeychuk.users.security.HospitalAuthenticationSuccessHandler" />

    <authentication-manager>
        <authentication-provider user-service-ref="hospitalUserDetailsService" >
        </authentication-provider>
    </authentication-manager>

</beans:beans>